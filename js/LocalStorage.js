var LocalStorage = /** @class */ (function () {
    function LocalStorage(listTasks) {
        this.listTasks = listTasks;
    }
    // createTask(task:Task):Task {
    //     return ;
    // }
    LocalStorage.prototype.selectAll = function () {
        return [];
    };
    LocalStorage.prototype.deleteTask = function (task) {
    };
    LocalStorage.prototype.deleteAllTask = function () {
    };
    LocalStorage.prototype.updateTask = function (task) {
    };
    return LocalStorage;
}());
export {};
